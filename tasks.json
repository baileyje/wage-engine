{
  
  "version": "2.0.0",
  "tasks": [
      {
          "label": "Build Run",
          "type": "shell",
          "command": "bazel build -c dbg --strip=never --verbose_failures --sandbox_debug //...",
          "windows": {
            "command": ".\\scripts\\test.cmd"
          },
          "cwd": "${workspaceFolder}/game",
          "group": "build", 
          "presentation": {
              "reveal": "always",
              "panel": "new"
          } 
      }
  ]
}