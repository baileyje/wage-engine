#include "entity/manager.h"

namespace wage {

  // EntityList EntityManager::with(std::string componentName) {
  //   EntityList found;
  //   for (auto entity : pool) {
  //     if (entity->getComponents()->get(componentName)) {
  //       found.push_back(entity);
  //     }
  //   }
  //   return found;
  // }

}